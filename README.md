# colors_extractor

A Python package for creating color palettes out of images.

## Usage

To run the project, you can use the command `python colors_extractor.py` in the command prompt or terminal.

You can try with any kind of images you want just put them into the samples folder and in the function call set
the first parameter with your image name.
